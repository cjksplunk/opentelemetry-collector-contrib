change_type: enhancement

component: receiver/mysqlreceiver

note: Add MySQL <8 and MariaDB compatibility for top-query collection by detecting the server version and using a fallback query template that omits the `query_sample_text` column, which does not exist in those versions. A shared plan cache prevents duplicate EXPLAIN calls across scrape cycles.

issues: [47302]

subtext: This issue covers only the Logs side of compatability for versions and platforms.

change_logs: [user]
