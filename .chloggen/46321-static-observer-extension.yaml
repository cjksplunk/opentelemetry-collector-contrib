change_type: new_component

component: extension/static_observer

note: Add `staticobserver` extension that fires a single synthetic endpoint, enabling `receiver_creator` to start subreceivers with per-instance resource attributes without modifying receiver code.

issues: [46321]

subtext:

change_logs: [user]
